import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'teamApt-WeightLoss-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
